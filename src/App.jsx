import React, { Component } from 'react';
import Navigation from './Navigation';
import MapView from './MapView';

const App = props => (
    <div>
        <Navigation />
        <MapView />
    </div>
);

export default App;

